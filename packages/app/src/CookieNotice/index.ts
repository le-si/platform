import { Modal } from "./Modal";
import { Provider } from "./Provider";

export declare namespace CookieNotice {
  export { Modal, Provider };
}

export namespace CookieNotice {
  CookieNotice.Modal = Modal;
  CookieNotice.Provider = Provider;
}
