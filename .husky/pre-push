#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"
. "$(dirname "$0")/common.sh"

# Ensure the entire project still compiles
blue=$(tput setaf 4)
yellow=$(tput setaf 3)
green=$(tput setaf 2)
normal=$(tput sgr0)
printf "%b" "${blue}Type checking${normal}${yellow} app${normal}..."
yarn app build:types
printf "%b" "${green}Success${normal}\n"

printf "%b" "${blue}Type checking${normal}${yellow} sdk${normal}..."
yarn sdk build:types
printf "%b" "${green}Success${normal}\n"
